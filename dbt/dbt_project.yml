name: 'lol_chamlp_perf'
version: '1.0'
config-version: 2

profile: 'lol_chamlp_perf'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  lol_chamlp_perf:

    staging:
      +materialized: view

    marts:
      +materialized: table

on-run-start:
  - "INSTALL httpfs"
  - "LOAD httpfs"